<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.jce.sena.taller1" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".IMCActivity"
            android:label="Índice de Masa Corporal" />
        <activity
            android:name=".VacacionesActivity"
            android:label="Calcular vacaciones" />
        <activity
            android:name=".CalculadoraActivity"
            android:label="Calculadora básica" />
        <activity
            android:name=".BienesRaicesActivity"
            android:label="Calcular couta inicial y mensual" />
        <activity
            android:name=".GetActivity"
            android:label="Gasto Energético Corporal" />
        <activity
            android:name=".AutosActivity"
            android:label="Autos" />
    </application>

</manifest>
